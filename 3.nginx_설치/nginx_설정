sudo yum localinstall -y --disablerepo=\* *.rpm
 
sudo mv /etc/nginx/conf.d/default.conf /etc/nginx/conf.d/default.conf.backup 

sudo vi /etc/nginx/conf.d/ftp.conf
---------------------------------------------------------------------------------

server {
    listen       8080;
    server_name  _;

    charset utf-8;
    access_log  /var/log/nginx/ftp.access.log  main;
    error_log   /var/log/nginx/ftp.error.log;

    root /u01/ftp/files;

    location / {
        autoindex on;
        autoindex_exact_size on;
        autoindex_localtime off;
    }

    #location ~* ^/.+\.(?:application)$ {
    #    default_type text/html;
    #    add_header Content-Type text/html;
    #}

    #location ~* ^/.+\.(?:exe|application)$ {
    #    add_header Content-disposition "attachment; filename=$1";
    #    default_type application/octet-stream;
    #    allow all;
    #}

    #location ~* ^/.+\..+$ {
    #    allow all;
    #    return 403;
    #}

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        allow all;
        root   /usr/share/nginx/html;
    }
}

------------------------------------------
sudo systemctl enable nginx
sudo systemctl restart nginx
systemctl status nginx

#만약 403에러 뜨면 파일시스템 권한에러임
 sudo chown -R adminftp:adminftp /u01/ftp/files/

 sudo vi /etc/selinux/config
SELINUX=disabled



